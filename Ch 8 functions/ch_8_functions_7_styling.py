def new_section():
	print()
	print('---------')
	print()


# Styling functions

# functions should have descriptive names, using lowercase letters and underscores

# have a docstring comment that explains presicely what the function does
# this thing: """right underneath the name of the function"""

# no spaces before or after = in parameters
def function_name(parameter_0, parameter_1='default value'):

# or arguments
function_name(value_0, parameter_1='value')

# indent accordingly so lines of code are not longer than 79 characters
def function_name(
	parameter_0, parameter_1, parameter_2,
	parameter_3, parameter_4, parameter_5):
	function body...

# import statements at the beginning of the file
# (with the exeption of comments)